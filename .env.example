VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_MOTION_APP_URL=http://localhost:3000

# Note: OPENAI_API_KEY belongs in volviq-motion-engine/.env (the dashboard app
# that actually calls the AI), not here — this landing site never calls OpenAI.
